# Codex — CALIBRATION SWEEP 3 (Stage 1.5) — from the 2026-07-08 live digest

Calibration-only, **separate from the web-app slices** (touch the evaluator/gates + config, not `web/`). All general rules, no brand-based score logic (the Palantir item is an explicit owner opt-out, not scoring). Source: `data/evaluation_set/live_calibration_notes.md` (2026-07-08). The post-`f965590` digest is structurally healthy (bands monotonic, locations/work-auth correct, no stale inflation) — these four fixes close real leaks where roles the owner *can't take* still surfaced.

1. **🔴 Language filter leaking — highest priority.** Two roles requiring a language the owner lacks surfaced as **Consider (72)** even though the eval prose calls it a "critical blocker": **Airwallex — Manager, Revenue Strategy, SG** (Mandarin *required*) and **Sierra — Strategist, Agent Development (Cantonese Speaking)** (Cantonese in the *title*). The owner speaks German (native) + English (fluent) only. A role that **requires** a working language outside {German, English} as a core requirement → **skip/block**, not consider.
   - Extend the language-marker detection beyond French/Spanish/Italian to Mandarin, Cantonese, and general title markers `(X Speaking)` / `(X-speaking)`.
   - Also read JD phrasing: "Fluency in <language> required", "<language> fluency is required/essential", "must be fluent in <language>". German/English requirements = no penalty.
   - Make the gate consistent with the prose: if the eval itself identifies a required-language blocker, the recommendation must be skip/blocked, never consider.

2. **🔴 Hard CS-degree + production-coding not disqualifying technical Deployment-Strategist variants.** Several **Mistral AI "AI Deployment Strategist"** postings state a CS/Engineering degree **requirement** and "proficient in developing code" / "hands-on building and deploying AI applications" as requirements, yet land at Consider (65–72). Per owner rule (a stated MUST-HAVE CS/Engineering degree or advanced/production coding = **hard disqualifier**), these must hard down-rank/block.
   - Distinguish two cases within the approved-stretch Deployment-Strategist family: (a) **business problem-solving > production engineering**, degree/coding are "preferred/plus" → keep as stretch/consider; (b) the JD states a CS/Eng **degree requirement** or **production-coding requirement** → treat as required-credential the candidate lacks → materially reduce fit / block, even inside the DS family.
   - This extends the existing required-credential handling; the tell is requirement language ("You hold a degree in…", "proficient in developing code", "hands-on building and deploying AI applications" as a stated requirement), not "ideal/plus."

3. **Dedup + company crowding.** Mistral "Deployment Strategist" surfaced **4×** across locations (EMEA / UK / Munich / Singapore) and **Airwallex ~7×**, together dominating the 25-slot cap.
   - Extend multi-location dedup (B-02) to collapse the same role across location variants into one card (list locations) — the Mistral DS family is the clear case.
   - Add a **per-company diversity cap** on the *surfaced* set (e.g. no single company consumes more than N of the shown cards; overflow rolls to the full list) so one employer can't crowd the digest. Keep it general, config-driven.

4. **Palantir → blanket skip (explicit owner decision 2026-07-08).** Add a temporary **company-level skip for Palantir** (all roles, not just gov/defense). Palantir Deployment Strategist – Spain (commercial "Echo") surfaced at Consider 76; the gov/defense rule correctly didn't fire, but the owner is opting out of Palantir entirely for now. Implement as a config-level employer opt-out (e.g. a `skip_companies`/blocklist in config) that's easy to revisit/remove — NOT hard-coded brand logic in the scorer. Distinct from the "no brand-based score inflation" principle: this is the owner declining an employer, not the model up/down-weighting a brand.

5. **✨ NEW — Estimated Level (Google-equivalent). Full spec: `docs/specs/estimated-level.md` — read it first.** Owner decision 2026-07-10: **seniority is a FLAG, not a FILTER; levels normalized to the owner's Google ladder; recall > precision.** Replace the current crude title-based over-level suppression with an estimate the owner can read.
   - **New evaluator outputs:** `estimated_level` (coarse: `L3|L4|L5|L6|L7+|unknown`), `level_confidence` (0–100), `level_rationale` (1–2 lines of evidence). Add as a dimension of the hybrid evaluator, same shape as `alignments`/`gaps`.
   - **Signals (no salary dependency):** (1) **years of experience required** — strongest universal signal; (2) **management scope** — senior IC / manages ICs / **manages managers** (manager-of-managers ⇒ L6+); (3) **reporting line** (reports to VP/CEO ⇒ senior); (4) **title × company size/stage** — the essential normalization ("Head of" at a 40-person startup is L4–L5; at a 5,000-person company it's a VP); (5) **posted salary band as a cross-check when present** (parser already exists) — bonus only, most EU/UK/SG/AU postings have none.
   - **Scoring:** delta from the owner's L4–L5 target ⇒ **modest fit down-rank only. NEVER suppress, never hard-block, never move a role to skip/blocked on level alone.** Bands stay strict/monotonic (#61) — fit remains the single source of truth; only the magnitude of the over-level penalty changes.
   - **Display (small A2 UI change):** role-card chip `est. L6 ▲ above band` (in-band neutral · above-band amber · below-band grey; low confidence ⇒ `est. L? · low confidence`), and `level_rationale` in the slide-over. Profile (A4) shows the target band.
   - **Guardrails:** always show confidence; coarse scale only (no "L5.3"); it's an estimate and will be wrong at startups/cross-geography — that's acceptable *because* it never blocks.
   - Live example to fix: **Quantium — "Executive Manager, Financial Services"** (Tier 3, fit 70, Consider) should surface with `est. L6 ▲ above band`, not be silently penalized or suppressed.

Re-run cached + live benchmarks (these change skip/consider composition; gated apply/consider precision should hold), push, back to Cato. Update `DECISIONS.md` for: the Palantir opt-out · the required-language / required-degree hard-disqualifier rules · **"seniority is a flag, not a filter; levels normalized to the Google ladder; recall > precision"**.

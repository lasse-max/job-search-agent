# Backlog — job-search-agent

Living backlog. Deferred with a reason; pulled in when it earns priority. (Team principle: defer explicitly, never gold-plate the MVP.)

| # | Item | Why deferred | Reconsider when |
|---|------|--------------|-----------------|
| B-01 | **Human-readable candidate one-pager** (`docs/candidate_one_pager.md`) — a recruiter/portfolio "about the candidate" view, *derived from* `config/candidate_profile.yaml` (YAML stays canonical; one-pager is a generated view to avoid drift). | Nice-to-have human artifact; the matcher already reads the structured config. | When Stage 1 is delivering value, or a recruiter/portfolio handout is needed. |
| B-02 | **Multi-location grouping (FR1-04)** — `canonical_key` is computed/stored but not used to group; location variants surface as separate digest rows. | Idempotency-by-`source_job_id` works; grouping is a Stage-1 add, not a B-blocker. | During Stage 1 build-out, before the digest gets noisy. |
| B-03 | **Notification history + de-dup (FR1-06)** — `notifications` table unwritten; `payload_hash` de-dup unexercised. | Only needed when real email send lands. | Checkpoint D (email delivery). |
| B-04 | **Digest "since last digest" + "don't resend" + richer cards (FR1-09)** — currently a full snapshot each run; cards omit confidence, tier/priority reason, source-posted date. | Acceptable for the dev digest. | Checkpoint D / when the digest goes live by email. |
| B-08 | **Declared-vs-implemented stack cleanup + dead templates (review m1).** | Repo hygiene item, but not blocking the source-run/evaluator integrity fixes. | Before broad Stage 1 expansion. |
| B-09 | **Scope README to implemented commands (review m4).** | Documentation polish; current pass is limited to review-blocking behavior. | Before the next checkpoint handoff. |
| B-11 | **Repository boundary + first migration (review m3).** | Structural boundary work is useful, but would expand this fix pass beyond the approved slice. | First persistence refactor / Stage 2 migration prep. |
| B-12 | **Pydantic schema validation for evaluator outputs (review m2).** | Lands naturally with the LLM evaluator, where schema enforcement matters most. | Checkpoint C LLM evaluator. |
| B-13 | **Wire `brand_floor` as a scoring/recommendation input with the LLM evaluator.** | The deterministic dev evaluator already uses company tier; nuanced prestige/trajectory floors are better judged with semantic context and calibrated examples. | When the LLM evaluator replaces the dev stub. |
| B-14 | **Email-sourced discovery (off-watchlist roles).** Ingest inbound job-alert emails (LinkedIn, Otta / Welcome to the Jungle, Built In, recruiter alerts) via the Stage-3 read-only Gmail access; parse company / role / location / apply-URL; run them through the same evaluator; surface high-fit hits in the digest flagged "off-watchlist — email-discovered," and optionally propose the company for the watchlist (`source_type = "email"`). Closes the blind spot where the fixed watchlist misses a strong role at a company the owner never thought to add. **Compliant: reads the owner's own emails via Gmail OAuth — NOT LinkedIn scraping (an explicit non-goal).** | Depends on Stage-3 Gmail OAuth; it's discovery-expansion beyond the MVP's fixed watchlist; the hard part is robust parsing of varied alert formats + leaning on the existing fit/visa/brand filters to cut alert noise. | After Stage 3 (Gmail) lands — aligns with the V2 "discovery expansion" roadmap. |

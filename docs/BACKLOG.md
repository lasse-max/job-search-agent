# Backlog — job-search-agent

Living backlog. Deferred with a reason; pulled in when it earns priority. (Team principle: defer explicitly, never gold-plate the MVP.)

| # | Item | Why deferred | Reconsider when |
|---|------|--------------|-----------------|
| B-01 | **Human-readable candidate one-pager** (`docs/candidate_one_pager.md`) — a recruiter/portfolio "about the candidate" view, *derived from* `config/candidate_profile.yaml` (YAML stays canonical; one-pager is a generated view to avoid drift). | Nice-to-have human artifact; the matcher already reads the structured config. | When Stage 1 is delivering value, or a recruiter/portfolio handout is needed. |
| B-02 | **Multi-location grouping (FR1-04)** — `canonical_key` is computed/stored but not used to group; location variants surface as separate digest rows. | Idempotency-by-`source_job_id` works; grouping is a Stage-1 add, not a B-blocker. | During Stage 1 build-out, before the digest gets noisy. |
| B-08 | **Declared-vs-implemented stack cleanup + dead templates (review m1).** | Repo hygiene item, but not blocking the source-run/evaluator integrity fixes. | Before broad Stage 1 expansion. |
| B-09 | **Scope README to implemented commands (review m4).** | Documentation polish; current pass is limited to review-blocking behavior. | Before the next checkpoint handoff. |
| B-11 | **Repository boundary + first migration (review m3).** | Structural boundary work is useful, but would expand this fix pass beyond the approved slice. | First persistence refactor / Stage 2 migration prep. |
| B-12 | **Pydantic schema validation for evaluator outputs (review m2).** | Lands naturally with the LLM evaluator, where schema enforcement matters most. | Checkpoint C LLM evaluator. |
| B-13 | **Wire `brand_floor` as a scoring/recommendation input with the LLM evaluator.** | The deterministic dev evaluator already uses company tier; nuanced prestige/trajectory floors are better judged with semantic context and calibrated examples. | When the LLM evaluator replaces the dev stub. |
| B-14 | **Email-sourced discovery (off-watchlist roles).** Ingest inbound job-alert emails (LinkedIn, Otta / Welcome to the Jungle, Built In, recruiter alerts) via the Stage-3 read-only Gmail access; parse company / role / location / apply-URL; run them through the same evaluator; surface high-fit hits in the digest flagged "off-watchlist — email-discovered," and optionally propose the company for the watchlist (`source_type = "email"`). Closes the blind spot where the fixed watchlist misses a strong role at a company the owner never thought to add. **Compliant: reads the owner's own emails via Gmail OAuth — NOT LinkedIn scraping (an explicit non-goal).** | Depends on Stage-3 Gmail OAuth; it's discovery-expansion beyond the MVP's fixed watchlist; the hard part is robust parsing of varied alert formats + leaning on the existing fit/visa/brand filters to cut alert noise. | After Stage 3 (Gmail) lands — aligns with the V2 "discovery expansion" roadmap. |
| B-15 | **Tune stretch-band precision.** Reduce noisy skip-to-stretch cases such as defense/government deployment roles the owner declines, talent acquisition, associate pricing, and other adjacent functions; grow the labelled positive set over time. | Fix Loop 3 keeps stretch cards visible for calibration monitoring, but only gates Apply/Consider precision for MVP. Stretch precision is measured separately and remains report-only. | After owner feedback from live stretch cards, before treating stretch as a trusted digest band. |
| B-16 | **Structured evaluator-failure diagnostics.** Persist machine-readable failure categories for each dropped/fallback role (missing key vs provider down vs malformed output vs spend cap), and surface aggregate counts separately from source health. | Fix Loop 5 records a human-readable skip reason and degraded source warning, which is enough to unblock live email; richer diagnostics deserve a small schema/design pass. | If live runs show recurring LLM wobble, provider instability, or unclear failure triage. |
| B-17 | **✅ RETIRED — digest calibration floor.** The app is now the browse surface and the evaluator is trusted. Email contains only genuinely new or materially changed fresh roles; quiet cycles send a zero-role scan heartbeat rather than repeating a top-five sample. Source-health/degraded warnings still send. | The feature reached its stated exit condition. The heartbeat proves the scanner ran without turning email back into a browse surface. | Closed by DECISIONS #80–81. |
| B-18 | **Outlook-specific digest degradation checks.** Inline-block chips/buttons and border-radius card styling can degrade in older Outlook clients even with the table layout and inline styles. Add Outlook-targeted render snapshots or a provider preview check before claiming pixel parity. | Current digest is readable and email-safe, but this is client-specific polish rather than MVP correctness. | Before investing in visual polish beyond "readable and branded" or if Outlook becomes the primary client. |
| B-19 | **Validate digest `source_url` schemes before rendering links.** Reject or neutralize non-HTTP(S) source URLs before they reach the HTML digest. | Current sources come from controlled ATS adapters/manual intake paths, and manual URL intake already rejects non-http(s); a render-layer guard would add defense in depth. | Next notification/security hardening pass. |
| B-20 | **Find Atlassian's real ATS feed.** `lever:atlassian` returns 0 jobs and is now disabled to avoid a false degraded source every run. Atlassian remains Tier 1, so promising roles should be captured through manual URL/text intake until the real feed/source key is identified. | Needs source research rather than product logic; manual intake covers the near-term opportunity risk. | Before treating Tier-1 source coverage as healthy. |
| B-21 | **Stale-evaluation backfill controls and visibility.** Add an explicit admin command/dashboard to count, batch, pause, and resume stale evaluator-version backfills by source. | The current fix auto-heals stale open postings opportunistically with a bounded per-scan batch and the monthly spend cap; that is enough to stop stale digest surfacing without adding Stage-2 control surfaces. | When persistent ops UI/admin tooling exists. |
| B-22 | **Structured compensation parsing beyond salary spans.** Extend pay-band parsing with currency conversion, location-adjusted thresholds, and broader examples for equity/OTE/contract-rate language. | The current parser fixes the false-positive class by reading only pay-adjacent spans; richer compensation semantics would expand calibration scope. | After more live examples show compensation is a frequent seniority signal. |
| B-23 | **Feedback-signal quality weighting (Stage 2.5 analytics — owner request 2026-07-08).** Use the owner's own pipeline actions as graded quality signals that tune scoring: actioning a recommendation nudges *similar* roles higher, and a signal ladder where **hotlist < mark-applied < interview** counts as progressively stronger positive evidence (dismiss / no-action = weak negative). Roles similar to ones the owner advanced get rated higher over time — a personalization/learning layer on top of the calibrated evaluator. **Guardrail:** learned signals stay subordinate to the hard gates (location / level / blockers) — a learned preference never overrides a blocker or the monotonic-band rule. | Needs the 1.5 tracker + pipeline states live to generate the action signals, plus enough action history to learn from; premature before there's a body of hotlist/apply/interview outcomes. Also risks reintroducing "brand/keyword inflation" if unconstrained — must be designed against the strict-bands discipline. | After 1.5 is in daily use and has accumulated real action history. Targeted as **Stage 2.5**. |
| B-27 | **🔴 SOURCE COVERAGE — 61 of 92 watchlist companies are dark (instrumented on the Profile page, 2026-07-11).** Coverage is **31/92 (34%)**: Tier 1 **10/20**, Tier 2 **13/45**, Tier 3 **8/27**. The dark set is not one problem: **44** have no identified source, **14** already have a supported Ashby feed but remain disabled, and **3** are known dead feeds (Atlassian, SafetyCulture, Vercel). Major dark targets include **Google · Apple · Amazon/AWS · Netflix · Uber · DoorDash · Canva · Atlassian · SafetyCulture · NEURA Robotics**. Highest-ROI sequence: validate and enable the 14 adapter-ready feeds in bounded batches; repair DoorDash, SafetyCulture, and Glean with newly identified existing-adapter sources; then build the multi-company adapter with the best tier-weighted return (SmartRecruiters currently appears to unlock Canva, Wise, Grab, ServiceNow, Nearmap, and Delivery Hero). Keep manual intake/B-14 alerts as an explicit bridge, not as automated coverage. | Not deferred by choice — this is the largest recall gap and was inherited from the Stage-0 audit. Binary coverage must also be paired with catalog-parity checks so a partial feed does not overstate recall. | **NOW — next after Calibration Sweep 3.** Optimize `tier weight × target-market relevance × expected gate-pass yield × freshness ÷ build/maintenance cost`; roll out 3–5 sources at a time and watch volume, LLM spend, runtime, and connector health. |
| B-29 | **✅ FIXED in `66bcd3b` — boilerplate false-merge silently deleted roles.** The old `description[:400]` key was replaced with the complete order-preserving, language/location-normalized role body plus the material-risk signature. Cato's shared-470-character-boilerplate repro now persists two distinct postings, while genuine location variants remain covered by the merge regression. | This blocker escaped twice before the standing-finding rule below was adopted. | Closed before the v4 bump. |
| 📌 | **PROCESS RULE (Otto, 2026-07-11, adopted after B-29 escaped twice): a blocker-class finding (🔴/🟠) stays on this board until it is either FIXED or FORMALLY ACCEPTED in `DECISIONS.md` with a stated reason. It never quietly ages out because a newer, shinier finding arrived.** Reviews are only as good as the triage that carries them forward. | — | Standing rule. |
| B-28 | **✅ FIXED — freshness is visible and actionable.** Potential Matches and To Apply cards show `posted Nd ago`, falling back explicitly to `first seen Nd ago`. The same config-driven 21-day policy now bounds evaluation/backfills and hides stale roles from default digest/web surfaces, with an explicit older-role audit toggle. | The early-application edge is now represented in both resource allocation and the UI. | Closed in the Stage 1.9 recency slice. |
| B-25 | **Role-card detail affordance is easy to miss (owner feedback 2026-07-10, first live use).** The "details ▾" toggle sits bottom-right of the card and the owner didn't spot it initially; the expand target is the middle of the card. Make the expand affordance more prominent / more obviously clickable (e.g. whole-card click target, clearer chevron, or a persistent "Why it fits" link). Not a blocker once you know it's there. | Cosmetic discoverability, not correctness; A2 works as designed. Better bundled with a broader UI polish pass once the 4 pages exist and real usage shows which affordances matter. | During the Phase-B usability pass, or the 2.0 polish layer. |
| B-26 | **Auth hardening beyond a single strong password (Cato 🔵 on `bf40afe`).** Password login is brute-forceable in a way the previous magic-link flow was not. Today the defence is a strong owner password + Supabase's built-in auth rate limits, which is proportionate for a private single-user tool. Before any external/multi-user exposure, add real protections: captcha on repeated failures, MFA/TOTP, login attempt monitoring, and a lockout/backoff policy. | Proportionate risk for one private user behind RLS + an allow-list of exactly one email; adding captcha/MFA now is friction with no attacker. | **Hard prerequisite for B-24 (external productization)** — do not expose this publicly without it. Also revisit if the app ever gets a custom domain or is linked anywhere public. |
| B-24 | **External productization — multi-user product with conversational onboarding (owner vision 2026-07-08).** Turn the single-user tracker into an external product (portfolio-grade, not self-serving): email login with owner-controlled access, and a setup flow where a **conversational/virtual intake agent** interviews a new user (citizenship, visa / work-authorization, target roles + level, CV upload) to build a candidate profile + watchlist like the owner's, then has them rate a handful of sample roles to seed per-user calibration. Demonstrates the project as a real product. | Large scope jump beyond the single-user 1.5/2.0 app: multi-tenancy, auth/access control, per-user profile + data isolation, CV parsing, conversational onboarding, and per-user calibration. Only worth building once the single-user product is genuinely good. | After 2.0 (single-user app polished), as a distinct **productization track** — later than the internal roadmap. |
| B-30 | **✅ FIXED — manual job entry with the full URL → text → manual fallback ladder.** Sextant records owner-gated submissions through migration 008. URL/text entries are consumed by the scheduled Python agent and use the existing manual normalizer, evaluator, gates, and current non-fallback provenance checks; they can land in Potential Matches, To Apply, or Applied. Failed extraction preserves the URL and requests pasted text. Last-resort company/title/link/note rows remain visibly `not evaluated`, never masquerading as calibrated scores. Off-watchlist entries can carry a watchlist proposal flag. | The web tier queues intent rather than duplicating Python scoring. | Closed in the B-30 slice; owner must apply migration 008 manually to activate the live queue. |
| B-31 | **Exceptional-match alert — the 90+ "drop everything" signal (owner 2026-07-12).** A fit **90+** is rare (Airbnb BizOps at 90 is the first ever recorded). Those are exactly the roles where the **96-hour golden hour** pays most. **Design questions before building:** (a) is a separate real-time alert worth it, or does the daily digest already surface it fast enough — the scan is daily, so "real-time" buys ≤24h; (b) if built, it must stay **rare by construction** — an alert that fires weekly becomes noise and gets ignored. Consider gating on `fit ≥ 90 AND fresh (<96h) AND Tier 1/2`; (c) the **visual treatment is worth doing regardless** — a 90 must not look like an 80 in the app or the digest. **Note:** the PRD already contemplates an "optional urgent alert" (Stage 1); this revives it with a much higher bar. | Owner's own read: the separate alert may be overkill since the daily email surfaces it anyway. The visual-highlight half is cheap and clearly worth it; the push-alert half needs justifying with data. | After Phase B shows how often 90+ actually occurs. Roughly monthly ⇒ an alert is justified. Weekly ⇒ it's noise; ship only the highlight. |

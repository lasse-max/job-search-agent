# Decision Log (ADR-style)

Each entry: decision, alternatives, reason, reversibility. New non-blocking assumptions made during the build are appended here rather than expanding scope. Consequential or blocking decisions are escalated to the owner.

| # | Date | Decision | Alternatives | Reason | Reversible? |
|---|------|----------|--------------|--------|-------------|
| 1 | 2026-06-23 | **Deterministic-first; LLM only for semantics** (interpretation, evidence mapping, gaps, summary). | Pure agentic loop. | Reliability + cost; normalization/diffing/blocking must not depend on a model. | Yes |
| 2 | 2026-06-23 | **ATS public APIs before scraping** (Greenhouse, Lever, Ashby). | Browser scraping everywhere. | ~80% coverage with no brittle automation; publish unsupported coverage instead of faking it. | Yes |
| 3 | 2026-06-23 | **Stage 1 is headless Python; SQLite state.** Web app deferred to Stage 2. | Build the Next.js + Supabase app first. | Right-sizes v1 to the builder (owner + coding agent, basic Python); ships value in days, not weeks. | Yes — repo/ORM boundaries keep a Postgres migration cheap. |
| 4 | 2026-06-23 | **Never write to the source spreadsheet in Stage 1.** | Two-way sync. | Avoids fragile sync; one-way controlled migration happens in Stage 2. | Yes |
| 5 | 2026-06-23 | **Human approval gates every consequential action.** No auto-add, auto-status, outreach, or submission. | Auto-add above a score threshold; auto-status from email. | Misclassification is costly; control is the product boundary. | No (core principle) |
| 6 | 2026-06-23 | **Four separate evaluation outputs** (fit · feasibility · strategic priority · recommendation). | One blended score. | Stops company prestige or location from hiding weak role fit. | Yes |
| 7 | 2026-06-23 | **Narrow true blockers; everything else is a penalty.** | Hard title-based exclusions. | Title-only blocks hide good roles with unusual titles; use penalties + the review queue. | Yes |
| 8 | 2026-06-23 | **Status from email, never from a dead URL.** | Infer "closed/rejected" when a posting 404s. | Postings get reposted and URLs persist; the inbox is the real signal (Stage 3). | No |
| 9 | 2026-06-23 | **No multi-agent framework.** A scheduled workflow + narrow services. | CrewAI / LangGraph multi-agent. | Simpler, cheaper, testable; avoids a novelty demo. | Yes |
| 10 | 2026-06-23 | **Benchmark calibrated on owner-labelled history** (~30 roles). | Trust the prompt as-is. | The differentiator is encoding the owner's judgment; measure it, don't assume it. | Yes |
| 11 | 2026-06-23 | **Email via a transactional provider; Gmail is read-only (Stage 3).** | Gmail app-password/SMTP to send. | Never request Gmail write scopes just to send a digest; least-privilege. | Yes |
| 12 | 2026-06-23 | **Supabase Cron for production scheduling (Stage 2); GitHub Actions only for CI + Stage 1.** | GitHub Actions as the production scheduler. | Scheduled GH runs can be delayed/dropped and disabled on inactive repos. | Yes |

> Template for new entries: **Decision · Alternatives · Reason · Reversibility · Owner approval (if needed).**

## Stage 0 import addendum

| # | Date | Decision | Alternatives | Reason | Reversible? |
|---|------|----------|--------------|--------|-------------|
| 13 | 2026-06-23 | **Use the earlier scaffold docs/data as the source for README, roadmap, decision log, architecture, evaluation docs, benchmark set, and tracker snapshot.** | Keep the generated Stage 0 docs only. | The owner identified these files as prior work that cannot be regenerated. | Yes |
| 14 | 2026-06-23 | **Keep generated code/config/audit files from the current Stage 0 build.** | Replace the whole repository with the earlier scaffold. | The owner asked to use prior versions only for specific files and keep the current code. | Yes |
| 15 | 2026-06-23 | **Include the authoritative PRD as `docs/PRD.md`.** | Leave the README link broken or point to Downloads. | The repo should be self-contained before pushing. | Yes |
| 16 | 2026-06-23 | **Push only to a private GitHub repository.** | Public portfolio repo immediately. | The current repo contains private tracker-derived strategy, warm-path indicators, and real job-search data. | Yes, after creating a sanitized demo dataset |
| 17 | 2026-06-23 | **Use a deterministic development evaluator for Checkpoint B.** | Block the vertical slice until live LLM credentials are configured. | This proves fetch, normalize, dedupe, persistence, review state, source health, availability, and digest behavior without spending tokens or requiring secrets. | Yes, replace with the schema-validated LLM evaluator in Stage 1 |
| 18 | 2026-06-23 | **Evaluate target-scope roles only in the first Databricks slice.** | Evaluate all fetched Databricks postings. | The live feed has hundreds of jobs; the first slice should prove relevance without creating a noisy digest. | Yes |
| 19 | 2026-06-23 | **Rewrite Git history to purge the real tracker and owner/path PII.** | Delete the file only in a new commit. | A normal delete would leave the private workbook and home path recoverable from earlier commits. | No, except by another history rewrite |
| 20 | 2026-06-23 | **Replace the real tracker with a schema-only sanitized workbook.** | Keep the real tracker in a private repository. | The reviewer identified the tracker as containing PII; the code only needs a representative workbook shape for now. | Yes |
| 21 | 2026-06-23 | **Commit scan run/health state before digest rendering.** | Keep scan and digest in one transaction. | A digest/template failure must not erase a successful fetch, posting upsert, evaluation pass, or source-run record. | Yes |
| 22 | 2026-06-23 | **Use a small configurable relevance pre-filter and record skip reasons.** | Score every fetched posting; or keep role-family signals hard-coded only. | The Databricks feed is large, but skipped jobs must be auditable. Full YAML-driven scoring remains deferred to Checkpoint C. | Yes |
| 23 | 2026-06-23 | **Stamp placeholder evaluator output as `uncalibrated_dev_stub_v1`.** | Keep `role_evaluation_v1`/policy literals as if the output were calibrated. | The deterministic evaluator proves the pipeline only; scores are not trusted until benchmarked against owner-labelled roles. | Yes |
| 24 | 2026-06-23 | **Apply the owner-approved visa stance in both config and deterministic feasibility.** | Keep Sydney/London as uncertain until full config loading. | The approved correction says only the US is high-friction; Australia timing is represented as `arrival_plus_3_months` because no arrival date is configured yet. | Yes |
| 25 | 2026-06-23 | **Cap stretch-family roles at consider/stretch unless a warm path or explicit exceptional-upside flag exists.** | Let raw fit score push Deployment Strategist/FDE-style roles to apply-now. | The reviewer flagged the stretch ladder as too optimistic; exceptional upside is a parameter, not auto-inferred in this dev evaluator. | Yes |
| 26 | 2026-06-23 | **Ashby C1 uses the live public `jobs` payload shape and `includeCompensation=false`.** | Infer shape from the audit only. | Live OpenAI/Airwallex pulls confirmed top-level `jobs` with posting `id`, title, department/team, employment type, locations, URLs, published date, and descriptions. The compensation flag keeps fixtures focused on discovery fields. | Yes |
| 27 | 2026-06-23 | **Ashby fetch sends a browser-like User-Agent.** | Use bare `urllib` headers. | The live Ashby endpoint returned 403 to bare Python urllib but succeeded with a normal User-Agent; this is connector compatibility, not scraping. | Yes |
| 28 | 2026-06-23 | **Lever source metadata can be derived, but Lever companies stay disabled until C2.** | Leave enabled audit rows runnable before an adapter exists. | C1 only ships Greenhouse + Ashby adapters; known Lever source keys remain useful evidence but must not be scanned until the Lever adapter is implemented. | Yes |
| 29 | 2026-06-23 | **CI is the standing independent green-suite check on every push.** | Rely on local-only test runs. | C2 requires a repeatable GitHub check using Python 3.12, unittest discovery, and Ruff when configured. | Yes |
| 30 | 2026-06-23 | **Lever C2 treats the live API as a top-level postings array.** | Wrap Lever payloads in an artificial `jobs` envelope. | Live Mistral payloads confirmed a list of postings with `id`, `text`, `categories`, `descriptionPlain`/HTML fields, URLs, and `createdAt`. | Yes |
| 31 | 2026-06-23 | **Normalize Lever `createdAt` from epoch milliseconds to UTC ISO timestamps.** | Store raw millisecond strings. | Canonical postings already use ISO-like source timestamps for Greenhouse/Ashby; preserving time while normalizing format improves cross-adapter consistency. | Yes |
| 32 | 2026-06-23 | **Use Lever `categories.allLocations` as the complete location set, with `categories.location` as fallback/dedupe.** | Store only the primary `location`. | Lever multi-location postings expose all target markets in `allLocations`; dropping them would hide relevant London/Munich/Singapore matches. | Yes |
| 33 | 2026-06-23 | **Enable companies only when their configured ATS adapter exists.** | Enable every audited supported-looking source at once. | C2 makes all Greenhouse and Lever rows runnable; unsupported/manual rows stay disabled, and non-requested Ashby expansion remains outside this slice. | Yes |
| 34 | 2026-06-23 | **Use the existing `prompt_version` column for the active scoring-policy version in C3.** | Add a new `scoring_policy_version_id` column now. | C3 must record all three config versions without starting the deferred migration/repository-boundary work; the evaluation JSON also carries explicit profile/location/scoring provenance. | Yes, replace with a dedicated column when the first migration lands |
| 35 | 2026-06-23 | **Load evaluator policy YAML with PyYAML, while keeping the watchlist parser scoped.** | Extend the hand-rolled parser to nested scoring/profile/location structures. | The project already declares PyYAML, and nested config needs real YAML parsing; the watchlist parser remains unchanged to avoid broad config-loader scope. | Yes |
| 36 | 2026-06-23 | **Surface `brand_floor` in the profile, but defer deterministic scoring to the LLM evaluator.** | Add a rule-based brand/trajectory penalty to the dev evaluator. | Company tier already carries deterministic brand priority; trajectory-step-down judgment needs semantic context and labelled examples, so it belongs with the calibrated LLM evaluator. | Yes |
| 37 | 2026-06-23 | **C4a manual URL extraction is best-effort HTML text extraction; failed URLs are preserved as `needs_text` intake requests.** | Build a browser/scraper extraction stack now, or silently drop failed URLs. | The slice is operability, not scraping reliability. A failed extraction should request pasted JD text and leave an auditable URL record without polluting the opportunity review queue. | Yes |
| 38 | 2026-06-23 | **C4b expected-volume degradation uses a 20% minimum threshold from audit count or last-known-good, with explicit fixture scans excluded from production baseline learning.** | Treat any drop as degraded, or ignore audit counts and learn only from live scans. | A 20% floor catches silent-zero/near-zero failures such as 757→1 without flagging normal feed variation; fixtures are partial test payloads and should not redefine production expectations. Audit rows with zero jobs seed a minimum of 1 because those rows were identified as suspicious silent-zero cases. | Yes |
| 39 | 2026-06-23 | **Stage 1 scheduled scans use GitHub Actions plus a SQLite cache/artifact, not a live email provider.** | Add a hosted database or email notification provider now. | C4b is unattended operation only; the workflow makes missed/failed runs visible and keeps local digest output available without starting Checkpoint D. | Yes |
| 40 | 2026-06-23 | **Treat the scheduled SQLite cache as discovery/keep-warm state only until review state is durable.** | Wire live email delivery to the cached GitHub Actions database now. | Local CLI review decisions do not automatically reach the scheduler's cache database, and cache eviction can reset scheduler state. Checkpoint D live email must not depend on cache-state review decisions; durable shared review state belongs in Stage 2 or an explicitly approved persistence upgrade. | Yes |
| 41 | 2026-06-23 | **D1 benchmark runs offline from committed JD snapshots.** | Live-fetch benchmark roles on every run. | Calibration must be CI-safe, reproducible, and independent of changing job-board pages; cache refresh is an explicit CLI option, not the default benchmark path. | Yes |
| 42 | 2026-06-23 | **Use deterministic calibration gates for US friction, clearance, technical-PM depth, junior scope, CS function, and tier-3 cold roles.** | Let raw fit score surface all high-scoring adjacent roles. | The labelled set showed perfect recall but low precision before calibration; these gates encode owner-approved judgment that visa blockers override fit, warm path softens but does not erase US friction, function beats title, and lower-priority brands should not crowd the digest. | Yes |
| 43 | 2026-06-24 | **D1 integrity fix: report exact recommendation match separately from recall.** | Keep only Apply/Consider recall as the headline. | Clean benchmark input now has no per-company clearance hardcode, no label-derived department, strict feasibility labels, and warm-path flags in the eval data. The current exact recommendation match is 22/33 (66.7%); `apply_now` vs `consider` remains approximate in the deterministic evaluator because scores cluster near thresholds, with fine ranking deferred to the LLM evaluator. | Yes |
| 44 | 2026-06-24 | **Keep narrow deterministic detectors explicit and guarded.** | Remove all domain-specific deterministic penalties until the LLM evaluator. | `_partnership_domain_gap` exists because card-network/banking partnership roles were owner-labelled as reach despite generic S&O wording. US location aliases include San Mateo and explicit `(US)` because they appear in the labelled set; ambiguous Glendale only counts as US when paired with `(US)`, California, or CA so non-US Glendales do not misfire. | Yes |
| 45 | 2026-06-24 | **Use Resend as the first transactional email provider behind a swappable interface.** | SendGrid, SMTP, or Gmail send scopes. | Resend is a small HTTP integration and keeps Gmail write scopes out of the MVP. If `RESEND_API_KEY` is absent, D2 keeps the local HTML/text fallback and records a fallback notification. | Yes |
| 46 | 2026-06-24 | **D2 since-last digest uses scheduled SQLite cache state, not durable Stage-2 review state.** | Add hosted persistence now. | This completes the MVP without starting Stage 2. Cache eviction resets notification history and since-last behavior; live email should not claim durable honoring of local review decisions until shared persistence exists. | Yes |
| 47 | 2026-06-24 | **Live email paused until live-noise precision clears.** | Keep scheduled Resend secrets wired while the evaluator is unvalidated on live noise. | The first live digest surfaced Payroll, Legal, and Sales roles as `apply_now`; live delivery on an unvalidated evaluator erodes trust. The scheduled workflow no longer passes `RESEND_API_KEY` or `DIGEST_RECIPIENT_EMAIL`, so `scan-all` falls back to local files until live-noise precision is at least 80% and Lasse signs off. | Yes |
| 48 | 2026-06-24 | **Use the curated 33-role benchmark for recall and the ~150-role live-noise set for precision.** | Continue treating curated benchmark precision as representative. | The curated set did not test rejecting the off-target majority of the live feed. The sampler writes a label template from cached live postings; once labelled, `job-agent benchmark` reports live-noise precision separately while retaining the curated set as the recall gate. | Yes |
| 49 | 2026-06-24 | **Hybrid Claude evaluator replaces `uncalibrated_dev_stub_v1`.** | Keep keyword-only deterministic scoring as the trusted evaluator. | Claude supplies validated role dimensions, role-specific alignments, gaps, confidence, summary, and an advisory band; code computes weighted fit, blockers, feasibility, and final recommendation. The deterministic fallback remains available for no-key local/CI paths and as a reversible safety valve. | Yes |
| 50 | 2026-06-24 | **Apply a deterministic posted-location gate before evaluation.** | Let all configured company locations reach the evaluator and rely on visa feasibility later. | Live-noise labels showed most noise is wrong-location. The gate allows the owner-confirmed Western Europe/Singapore/Sydney/Melbourne markets, treats Dublin as tier-1-only, skips non-target geographies, and lets US roles through only for tier-1 companies with explicit sponsorship and exceptional role fit. Visa feasibility remains a separate evaluation output. | Yes |
| 51 | 2026-06-24 | **Treat stated must-have technical requirements as hard blockers.** | Penalize them as ordinary gaps or rely only on title-based technical blockers. | A required CS/engineering degree, advanced/professional programming, production software development, or deep ML/data-science engineering is outside the candidate's current profile when stated as a minimum. Preferred, bonus, familiarity, or exposure language does not fire the blocker. Claude may emit quoted evidence, but code enforces the final `blocked` override. | Yes |
| 52 | 2026-06-25 | **Apply an L4-L5 seniority ceiling.** | Let strong function fit surface Head/Director/VP/C-suite roles. | The target band is Manager, Senior Manager, Lead, or Senior IC. At established companies, Head of, Director, VP, and C-suite roles are over-leveled and should skip on level even when the function matches; small-startup Head-of roles remain eligible when scope is roughly L4-L5. | Yes |
| 53 | 2026-06-24 | **Model-spend tracking remains cache-local until durable state exists.** | Add a durable spend ledger now. | The current JSON ledger protects local/scheduled runs from accidental over-spend but can be reset by cache eviction or a fresh environment. Durable persistence belongs with Stage 2 shared state; this slice only corrects the per-role estimate and documents the limitation. | Yes |
| 54 | 2026-06-25 | **Calibrate the hybrid Claude combiner with core-family floors and low-priority surface caps.** | Trust raw Claude dimensions only, or lower global thresholds. | The populated cache showed Claude Haiku under-scored obvious S&O/BizOps title/department matches while over-surfacing adjacent functions such as strategic finance, technical account management, legal/risk ops, talent acquisition, plain RevOps, and government deployment roles. Floors protect curated Apply/Consider recall; caps protect live-noise digest precision without changing blockers or feasibility. Current cached benchmark: curated Apply/Consider recall 100%, live-noise precision 100%. | Yes |
| 55 | 2026-06-25 | **Gate live-noise calibration on recall and precision; narrow caps/blockers to the role function.** | Keep precision-only reporting, broad domain caps, and raw LLM hard-blocker enforcement. | Precision-only hid missed Apply/Consider roles. The live gate now requires Apply/Consider recall >=90% and precision >=80%. Banking/payment/partnership words in JD boilerplate no longer cap core Revenue Strategy & Operations roles; low-priority caps use title/department function. Degree-only hard blockers are not enforced for approved Deployment Strategist stretch roles unless the source also states production coding, advanced programming, or deep ML/data-science engineering as a hard requirement. | Yes |
| 56 | 2026-06-25 | **Keep full stretch cards visible, but report their precision separately.** | Hide/collapse stretch cards until calibrated, or gate MVP email on all-surfaced precision. | Owner wants stretch roles in the digest as a calibration-monitoring surface. Apply/Consider precision remains the gated >=80% metric; all-surfaced precision including stretch is report-only until the stretch band is tuned. | Yes |
| 57 | 2026-06-25 | **Technical-depth hard requirements self-enforce; core-family calibration remains audited.** | Require literal "required" phrasing for every hard requirement, or remove the core-family floor entirely. | Production coding, production software development, advanced programming, and deep ML engineering are true blockers when central, even without the exact word "required"; degree-only blockers remain suppressible for approved stretch roles. The core-family floor is retained because removing it drops owner-labelled curated recall, but it does not create LNP-090's apply-now result: the cached Claude dimensions for that role are already strong (87/82/79/75), and low-priority guards still fence plain RevOps/adjacent functions. Carry-over checks remain covered by tests: fallback email refusal and sub-cent Haiku cost estimate. The live-noise precision set has 10 unlabeled positive-candidate rows added for Lasse; current metrics still use the 150 labelled rows only. | Yes |
| 58 | 2026-06-25 | **Malformed single-role LLM outputs degrade that source, not the whole digest.** | Treat any malformed LLM output as deterministic fallback, or fail the whole scheduled scan immediately. | Live Claude can occasionally return stringified JSON for list fields. The evaluator now coerces those list fields; if validation still fails, the scan retries once. Partial failures drop only the bad role with an evaluation-skip reason; wholesale failures fall back to deterministic evaluations and mark the source degraded so the digest can still send with a clear warning and cap. | Yes |
| 59 | 2026-06-26 | **Use one capped email digest path for normal and degraded evaluator modes.** | Block degraded/all-fallback emails to zero, or use a smaller degraded-mode cap. | The live run leaked 93 roles because the email path capped per section instead of enforcing a total cap. The email now selects rows before rendering: normal mode sends only validated LLM rows, degraded mode sends fallback rows, both ranked and capped at 25 with an absolute max of 50. Overflow is explicit, fallback rows are counted, and degraded emails are labeled in subject/body rather than silently blocked. | Yes |
| 60 | 2026-06-27 | **Run the scheduled scan once daily and append a normal-mode calibration floor when fewer than five strong roles surface.** | Keep the 6-hour cadence and allow quiet/empty normal digests to suppress or show only the standard low-priority compact list. | The live digest is more useful as one morning check-in than as four inbox events per day; the tradeoff is up to <24h discovery latency for lower noise. During live calibration, the owner still needs something inspectable even when few roles clear apply/consider/stretch, so normal, non-degraded digests append a clearly labelled "Top open roles by fit — may repeat" section with up to five top-fit current open roles regardless of band. This intentionally overrides since-last suppression, while the degraded all-fallback path remains separate. | Yes, restore the 6-hour cadence or remove the sample once live calibration is trusted |
| 61 | 2026-06-29 | **Make the final recommendation band a strict function of final fit score.** | Keep post-hoc tier/warm-path/stretch-family overrides that can raise a lower-fit role above a higher-fit role. | Live calibration showed non-monotonic output: a lower-fit warm-path/Tier-1 role could become `apply_now` while a higher-fit role stayed `consider`. Final bands are now 80+ `apply_now`, 70-79 `consider`, 60-69 `stretch`, and <60 `skip`; blockers, infeasible status, over-level roles, and fit-input penalties can only push downward. Tier, warm path, and function risk feed the score rather than jumping the band. | Yes |
| 62 | 2026-06-27 | **Disable the dead Atlassian Lever source and rely on manual fallback until the real ATS feed is found.** | Leave `lever:atlassian` enabled and accept a degraded warning every run. | The configured Lever source has returned 0 jobs since audit, creating a false degraded signal on every scheduled scan. Atlassian stays Tier 1, but promising roles should enter through manual URL/text intake while the real feed/source key is researched. | Yes |
| 63 | 2026-06-30 | **Apply B-15 calibration filters as general role rules, not brand logic.** | Tune individual companies or labelled rows. | Unsupported core language requirements now skip unless the required language is in the candidate profile; government, defense, and clearance work is declined when core to the role; pre-sales/value-engineering and adjacent ops noise are capped from title/department function; language variants dedupe to one base role. Product monetisation/pricing in a Product function is treated as product strategy, while standalone pricing/finance noise remains down-ranked. | Yes |
| 64 | 2026-07-04 | **Make fit the single source of truth for gate penalties.** | Keep relevance/skip gates separate from the numeric score. | Live calibration showed stale high-fit skipped roles in the calibration floor. Off-location, off-function, wrong-language, government/defense/clearance, required-credential, and over-level signals now reduce fit directly; posted compensation bands above the L4-L5 target cap senior-IC Lead roles to stretch and executive-level roles below the bar. LLM persistence keys include evaluator calibration version so the next scan refreshes pre-recalibration scores. | Yes |
| 65 | 2026-07-08 | **Backfill stale evaluator-version rows automatically, but cap each scan.** | Add a one-off manual migration only, or re-evaluate the full stale cache in one run. | The live cache can contain open postings scored by an older calibrated evaluator version. Each scan now appends a bounded set of stale-version open postings after new/changed work, writes only current calibrated LLM evaluations, and keeps stale rows out of digest reads until refreshed. The monthly spend cap remains authoritative; large stale caches heal across runs. | Yes, replace with an explicit admin backfill UI/command when Stage 2 persistence lands |
| 66 | 2026-07-09 | **Make Supabase Postgres opt-in via `JOB_AGENT_DATABASE_URL` while keeping SQLite as the local fallback.** | Replace all SQLite paths immediately or add a separate sync-only service. | Stage 1.5 step 1 needs the scanner capable of writing the shared Postgres store, but local tests and existing workflows still depend on SQLite. The scanner now uses Postgres when the URL is provided; otherwise it behaves exactly as before. The web app reads only the Postgres calibrated views and does not re-implement scoring. | Yes, once Supabase is the only production store |
